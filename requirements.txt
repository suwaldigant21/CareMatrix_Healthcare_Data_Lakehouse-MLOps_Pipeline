# CareMatrix analytics + ML pipeline (install with `uv pip install -r requirements.txt`)
# Versions match the tested environment (Python 3.14, Windows, uv-managed venv).
dbt-athena==1.11.0
dbt-core==1.12.0
pyathena==3.34.0
pandas==3.0.5
numpy==2.5.1
scikit-learn==1.9.0
xgboost==3.4.0
joblib==1.5.3

# Local mock claims API (mock_api/ has its own pinned requirements.txt for Docker)
